# Development and test dependencies
# Install alongside production deps:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.4.0
mypy>=1.10.0
