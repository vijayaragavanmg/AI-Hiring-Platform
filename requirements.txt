# ── Core: LangChain ecosystem ──────────────────────────────────────────────
langchain>=1.2.13
langchain-community>=0.4.1
langchain-google-genai>=4.2.1
langchain-openai>=1.1.11

# ── LangGraph ──────────────────────────────────────────────────────────────
langgraph>=1.1.3

# ── Vector store ───────────────────────────────────────────────────────────
chromadb>=1.5.5

# Used by CrossEncoderReranker to validate torch version at import time
packaging>=23.0

# ── Reranking (cloud) ─────────────────────────────────────────────────────────
# Used by: CohereReranker
cohere>=5.20.7

# ── Document loaders ───────────────────────────────────────────────────────
pypdf>=6.9.1
docx2txt>=0.9

# ── API ────────────────────────────────────────────────────────────────────
fastapi>=0.135.1
uvicorn[standard]>=0.42.0
python-multipart>=0.0.22
aiofiles>=25.1.0

# ── Data validation ────────────────────────────────────────────────────────
pydantic>=2.12.5

# ── Environment variables ──────────────────────────────────────────────────
python-dotenv>=1.2.2

# ── Dev / test (install with: pip install -r requirements.txt -r requirements-dev.txt) ──
# See requirements-dev.txt
